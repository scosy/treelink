<h1>Editing link</h1>

<%= render "form", page: @page, link: @link %>

<br>

<div>
  <%= link_to "Show this link", page_link_path(@page.id, @link.id) %> |
  <%= link_to "Back to links", page_links_path(@page.id) %>
</div>
