<p style="color: green"><%= notice %></p>

<h1>Links</h1>

<div id="links">
  <% @links.each do |link| %>
    <%= render link %>
    <p>
      <%= link_to "Show this link", page_link_path(@page, link) %>
    </p>
  <% end %>
</div>

<%= link_to "New link", new_page_link_path %>
